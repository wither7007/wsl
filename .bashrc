# If set, the pattern "**" used in a pathname expansion context will
source ~/u.sh
export PATH=/home/steff007:$PATH
export PATH=/home/steff007/.local/bin:$PATH
echo $PATH
echo "cat /etc/os-release #version"
cat /etc/os-release #version
